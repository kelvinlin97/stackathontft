import React from "react";
import ItemDisplayColumn from "./ItemDisplayColumn";
import ItemDisplayRow from "./ItemDiplayRow-component/ItemDisplayRow";

const ItemDisplay = (props) => {
  let row = [];
  let counter = 0;
  for (let i = 0; i <= 9; i++) {
    row.push() < ItemMatrixRow;
  }
};
